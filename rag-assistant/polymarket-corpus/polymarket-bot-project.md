---
name: polymarket-bot-project
description: "Polymarket-Trading-Bot Vorhaben — Bullpen CLI + Kronos-Signal + Binance-Bot, Krypto Up/Down-Märkte"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7a7f46ce-2ccb-44bf-8a4f-f3d364d8b0c0
---

Vorhaben (gestartet 2026-06-04): Polymarket-Bot, Fokus Krypto-Spalte (evtl. auch Sport). Soll Wetten automatisch öffnen und Gewinne redeemen/claimen.

**Bausteine (alle vorhanden):**
- **Bullpen CLI** v0.1.99 in WSL-Ubuntu: `~/.bullpen/bin/bullpen` (User `cosmic-canary`). Deckt fast alles ab: `polymarket discover crypto|sports`, `buy`/`sell`, `limit-buy`, `redeem`, `redeem-neg-risk`, `rewards`, `candles`, `price-history`, `orderbook`, `watch` (WS), `--output json`. `buy --wallet 0x.. --yes --max-price` ist explizit für Bot-Automation gedacht. → Kein eigener Polymarket-Client nötig, nur Orchestrator drumherum.
- **Kronos** (= kronosbook-yyqavmxb.manus.space) in `~/Kronos/` (Git-Repo) + venv `~/kronos-venv/`. OHLCV/K-Line Foundation-Model (AAAI 2026), WebUI unter `~/Kronos/webui/app.py`. Liefert das Up/Down-Wahrscheinlichkeitssignal. Siehe [[trading-bot-project]] für Binance-Teile (Momentum/Funding/Vol, 15min).

**Markt-Slug-Muster (Krypto Up/Down, deterministisch baubar):** intraday `<prefix>-updown-{5m,15m}-<unix>`, daily `<name>-up-or-down-on-<date>`. Outcome Up/Down = Yes/No, Preis = implizite Wahrscheinlichkeit.

**Multi-Coin (2026-06-04):** Bot deckt jetzt **6 Coins** ab: BTC, ETH, SOL, XRP, DOGE, BNB (`config.ASSETS`: name=Daily-Slug, prefix=Intraday-Slug, symbol=Binance). Daily-Muster für ALLE einheitlich `<name>-up-or-down-on-<date>` (bitcoin/ethereum/solana/xrp/dogecoin/bnb). `polymarket.slug_for(tf, now, asset)`, `runner.scan_once(assets, tfs, models)`, `evaluate_tf(...asset)`. CLI: `runner.py scan --asset BTC ETH SOL ...`. Dashboard scannt alle 6 × [daily,15m] (5m aus wg. Last), ~105s/Scan, Coin-Spalte. Cardano/Avalanche haben kaum Up/Down-Märkte.

**Wallet-Status:** seit 2026-06-04 **eingeloggt & bereit** (`wallet_ready:true`, `onboarding_complete:true`, `token_valid:true`). Echtes Trading freigeschaltet.

**Copy-Trading (nativ in bullpen!):** `tracker copy start <addr> --exit-behavior mirror_sells --execution-mode confirm|auto` spiegelt Käufe UND Verkäufe automatisch; Risk-Caps `--max-trade-size/--daily-limit/--budget/--max-per-market/--mirror-percent-cap/--amount`. Top-Trader via `polymarket data leaderboard --sort pnl|copyability|win-rate --time-period 30d`. **Schlüssel-Erkenntnis: höchster PnL ≠ kopierbar** — Top-PnL-Trader sind Whales/Bots (`not_recommended`/`bot`, degen, dd 0.7-1.0). Praktisch kopierbar sind nur `highly_copyable` (kleiner PnL, dd 0.06-0.23, low/moderate risk). Selektor `~/polymarket-bot/copy_trader.py recommend` vereint Lenses + filtert (kein Bot, Tier highly_copyable/copyable, winrate≥0.50, pnl≥100, dd≤0.4); `start <addr...> [--execute]` (Default dry, --execute fügt --yes hinzu).

**ECHTES Copy-Trading LIVE seit 2026-06-04** für 3 Trader: `0x91e8a6edec03e7a81c88621123ebd041cb5ef1ab` (#1, $10.5k PnL, 65% WR, Politics) + `0xe47c17e97547de654ac80749286ad44d0e213afa` (#2, $2.3k PnL, 54% WR, low risk) + `0xeccf1ecfcf3ffe049d48f60b4697ca91e8256603` (#3 Krypto-Spezialist, $52 PnL, 75% WR, highly_copyable). **Erkenntnis: Krypto-Spezialisten haben winzige PnL** (Krypto-Up/Down ist kleinvolumig); `data smart-money --category crypto --type top_traders` liefert 0. Krypto-Trader nur via `copy_trader.py recommend --category crypto` (filtert primary_category, lockerere Schwellen `category_min_pnl/winrate`).

**Hot-Hand-Modus (2026-06-04):** `copy_trader.py recommend --today` (`rank_today`) — heute aktive Trader im Plus: `realized_pnl_1d≥1000`, `win_rate_1d≥0.55`, **`realized_pnl_30d≥0`** (verhindert „Verlierer mit Glückstag" — ein Whale war heute +$397k aber 30d −$933k!), recent_trade heute, keine Bots, Tier-Filter gelockert (Momentum statt Langzeit). Config `today_*`. **2 weitere live geschaltet (jetzt 5 Subs total):** `0x0dcf5b49627c5194805bd1af846fd0bd3388c556` (+$1.07M/30d) + `0xf1528f12e645462c344799b62b1b421a6a4c64aa` (+$414k/30d), beide Sports-Whales. **ACHTUNG: Subs × $25 Cap, aber Wallet nur ~$42** → Copies konkurrieren um dieselben $42. **Stop braucht ADRESSE nicht ID** (`tracker copy stop <addr> --yes`). **Am 2026-06-04 auf 3 reduziert:** aktiv = 0x0dcf5b49 (+$1.07M), 0xf1528f12 (+$414k), 0x91e8a6ed (highly_copyable $10.5k); gestoppt = 0xeccf1ecf (crypto), 0xe47c17e9 (whale#2).

**Backtest/Kalibrierungs-Layer (2026-06-04):** `~/polymarket-bot/backtest.py --days N --samples K --asset ...` misst Richtungs-Trefferquote + Kalibrierung von Kronos+Ensemble auf Daily Up/Down (16:00→16:00 UTC) je Coin. Historische Polymarket-Preise NICHT verfügbar → nur Richtungs-Hitrate messbar. Benchmarks: **Breakeven nach 10% Fee bei 0.50-Einstieg = 52.6%**, plus Naive-Baseline max(up_rate,1-up_rate) — Signal muss BEIDE schlagen. `futures_signal.signal_from_closes()` für historische Slices (ohne HTTP). Smoke-Test (n=7, BTC, starker Abwärtstrend): Hitrate 42.9% < Baseline 85.7% → in Trendphasen schlägt „immer Down" das Modell; braucht längere/gemischtere Perioden für faires Urteil. Voller 30d-Lauf → `backtest_result.txt`. **ERGEBNIS (alle 6 Coins, 30d): KEINE EDGE.** Trefferquoten BTC 46.7%, ETH 46.7%, SOL 50%, XRP 50%, DOGE 43.3% — alle unter Baseline (57-67%) UND unter Breakeven 52.6%. Ensemble-agree noch schlechter (43-46%). Kalibrierung invertiert (hohe Konfidenz → oft falsch), Brier 0.29-0.32 schlechter als konstant 0.50. DOGE entscheidend: up-rate 60% aber Modell nur 43% → kein Regime-Artefakt, echtes Fehlen von Können. **Klar: kein Echtgeld auf die Kronos+Ensemble-Signale.**

**Extrem-Quoten-Filter (2026-06-04):** Paper-Trade-Analyse zeigte ein Leck — Bot kaufte „Down" zu ask 0.01 (tote Märkte, unendliche Liquidität angenommen) → 90x-Payout-Artefakt ($10→$901), Konto unrealistisch aufgebläht. Fix: `edge.evaluate(min_price=0.10, max_price=0.90)` (config `min_ask`/`max_ask`) lehnt ask außerhalb [0.10,0.90] ab (quasi-aufgelöste Märkte = Rauschen). Verifiziert: ask 0.01/0.95 abgelehnt, 0.40 erlaubt.

**Copy-Status: GESTOPPT (2026-06-04, verlustreich).** Nach max-trade-size-Fix liefen 2 echte Copies: beide $10 auf „Aurora" (Dota2-Esports, Whale 0x0dcf5b), Match verlor → ~$20 weg, Guthaben $41.75→$21.23 (−48% in 2 Trades!). User-Feedback „verlustreich" → ALLE 5 Subs gestoppt. Lehre: mit ~$42 ist Copy von Whales ($5000-Tickets) zu volatil, ein Verlierer frisst die halbe Kasse.

**★ ECHTE EDGE GEFUNDEN (2026-06-04): Mean-Reversion nach grossen Moves.** `edge_research.py` (reine Statistik, kein Kronos) gepoolt über alle 6 Coins: nach Move ≥1.5-2% über L Bars dreht der Kurs über die nächsten ~3 Bars zurück. **REVERSION trifft 60-65%** (n=294-576), Momentum verliert spiegelbildlich (~35%). **59/78 Regeln schlagen Breakeven 52.6%.** Beste: 1h L=3 thr=1.5% H=3 → 62.1% (n=467); 1h L=4 thr=2% H=3 → 64.3% (n=294); 15m L=4 thr=2% H=3 → 65% (n=80). Krypto überreagiert + retraced. KONTRAST zu Kronos (47-50%, keine Edge). VORBEHALTE: (1) auf Binance-Kurs, nicht Polymarket-Preis gemessen — echter Test = preist Markt die Reversion schon ein? (2) 3h-Horizont mappt am ehesten auf 15m-Märkte; (3) nur ~60d/ein Regime. NÄCHSTER SCHRITT: Reversion-Strategie gegen echte Marktpreise testen.

**Kronos-base-Backtest:** läuft (BTC/ETH/SOL, 20d, samples 8) → `backtest_base.txt`.

**Reversion-Signal gebaut + integriert (2026-06-04):** `reversion_signal.py` (`signal()`): Move über `REV.lookback`(3) Bars `REV.interval`(1h); ab `REV.threshold`(1.5%) → Gegenrichtung, p_up kalibriert auf `REV.hitrate`(0.62) × conf. `config.RevSettings`/`REV` + `Settings.signal_mode` ("reversion"|"kronos"|"ensemble", Default reversion). `runner._eval_reversion()` + Branch in `evaluate_tf`; daily ungeeignet (übersprungen), nur 5m/15m. Dashboard `TFS=["15m","5m"]`. **KERNERKENNTNIS: Markt preist die Reversion großteils ein** — alle Coins gefallen → Signal „Up" 0.55-0.60, aber Markt-Up-Ask schon 0.57-0.86 → nach 10% Fee fast alles negativ-EV, Bot lehnt korrekt ab (XRP am nächsten, EV +0.007). Signal directional richtig, aber Markt effizient genug, dass Edge nach Fee selten. Bot tradet nur bei echter Fehlbewertung. Stündliche Märkte existieren NICHT mehr (nur Feb-Archiv). **Aktive Krypto-Märkte: nur 5m+15m updown, daily up-or-down, Preisziele (`bitcoin-above-on-<date>`, `will-bitcoin-reach-Xk`, `what-price-will-bitcoin-hit`).** 30 aktive Events via `discover crypto`.

**2 Fixes umgesetzt (2026-06-04) nach Trade-Analyse:** Analyse der 83 Paper-Trades zeigte: scheinbares Break-even (−$1.17) kaschierte ECHTE Trefferquote 22% (resolve-Exits); die 85% „sell"-Gewinne waren manuelles Cherry-Picking (Dashboard-Sell-Button) + Bid-Rauschen. ALLE 83 Trades „Up" (Abwärtstrend → Reversion sagt immer Bounce, der nicht kommt → Reversion versagt im Trend). FIX 1 **Regime-Gate**: `reversion_signal` nutzt Kaufman ER (`REV.er_window=10`, `REV.max_er=0.45`) — ER>0.45=Trend → kein Trade (nur choppy). FIX 2 **Preisfilter enger**: `min_ask=0.30, max_ask=0.70` (keine Quasi-Extrem-Quoten). `analyze.py` = wiederverwendbares Trade-Analyse-Tool (Trefferquote/P&L nach coin/tf/side/exit). **Kronos endgültig raus** (signal_mode=reversion, nirgends aufgerufen).

**Automatischer Tages-Report + Persistenz (2026-06-04):** `daily_report.py` (Paper-Analyse via analyze + Copy-Status + Guthaben → `reports/<datum>.txt` + Trend-Zeile in `daily_log.txt`, startet Dashboard falls down). `keepalive.sh` (HTTP-Health-Check via curl -m5; bei keiner Antwort `pkill -f dashboard.py` + `setsid python dashboard.py </dev/null >dash.log &` — räumt Zombies, volle Ablösung. +x nötig, Edit setzt +x zurück!). WSL2 bleibt durch systemd am Leben; nohup stirbt cross-invocation, setsid+cron überlebt. Kein passwortloses sudo → kein systemd-Service möglich.

**★ DECKEN-AUTO-VERKAUF + Paper-Trade-Klärung (2026-06-06):** Paper-Analyse zeigte 21 Trades, 100% Treffer, +$96 — sah nach Artefakt aus. Sell-Quelle: NUR `acc.sell()` von take_profit(aus, tp=0) + Dashboard-Button → die 21 waren MANUELLE Verkäufe des Users. User klärte: er verkauft teuer eröffnete Favoriten wenn der Bid Richtung Decke läuft (kaum Restspanne) — RATIONAL (nahe 0.90-0.95 verkaufen = fast voller Wert ohne ~6% Verlustrisiko, sogar leicht besser als Halten lt. Bias-Studie). 18 offen laufen noch. **FEATURE (b) gebaut:** `resolver.ceiling_take_profit(ceiling=SETTINGS.ceiling_sell=0.92)` — verkauft FAVORITEN automatisch wenn Live-Bid >= 0.92, LONGSHOTS AUSGENOMMEN (laufen bis Auflösung; User: die niedrig-Einstiegs-Gewinner haben die größte PnL → mehr Daten sammeln). `paper_engine.sell(reason=)` taggt Exit ('ceiling' vs 'sell'). In `dashboard.manage_loop` alle 60s. **BIAS-UPDATE:** 4222 aufgelöst; 0.85-0.95-Favoriten-Edge festigt sich (+3.1% EV, n=153, ~2.1σ); Longshots 0-0.15 solide -1.9% EV. User-Beobachtung: 0.30-0.45-Bucket zeigt Edge-Stern HEUTE+VORGESTERN (wiederkehrend → evtl. echte kleine Sekundär-Edge +0.6% EV, n=311, beobachten). Feature (a) FV-Bot auf 0.85-0.92 schärfen noch offen.

**Take-Profit/Stop-Loss (2026-06-04):** `SETTINGS.take_profit_pct=0.20` (verkauft Position bei +20% zum Live-Bid), `stop_loss_pct=0.0` (aus). `resolver.take_profit(tp,sl)` prüft jede offene Position gegen best_bid, verkauft via `acc.sell`. Dashboard `manage_loop` (ex copy_loop, alle 60s) ruft `take_profit()` + `resolve_due()` + `fetch_copy_live()` — häufig genug für 5m-Märkte. `LATEST["last_tp"]` für Anzeige. WSL hat systemd+cron (enabled). Crontab (`crontab.txt`): keepalive alle 10min, @reboot keepalive, Report 08:00 Berlin. Windows-Logon-Task „PolymarketBot" (`schtasks`) startet WSL+keepalive bei Anmeldung → cron läuft nach Reboot weiter. **★ NETZWERK+PERSISTENZ KOMPLETT GEFIXT (2026-06-04):** „Dashboard geht nicht" hatte 2 Ursachen. (1) **WSL-localhost-Forwarding (NAT) kaputt** → Windows kam nicht ran. FIX: `.wslconfig` (`C:\Users\petra\.wslconfig`) mit `[wsl2] networkingMode=mirrored` + `wsl --shutdown` → WSL teilt Windows-Netz (eth0=192.168.2.92). **ZUGRIFF NUR via http://127.0.0.1:8787** (NICHT localhost — IPv6 ::1 nicht gebunden, timeout). Dashboard bindet `0.0.0.0`. (2) **Persistenz:** `run_dashboard.sh` (while-loop, flock `/tmp/run_dashboard.lock` = nur EINE Instanz) startet Dashboard neu wenn's endet. Gestartet via `polymarket_launch.vbs` (`C:\Users\petra\`, WScript hidden window 0) → `wsl.exe -- bash /home/.../run_dashboard.sh` (BASH explizit, weil Windows-Edits +x-Bit zurücksetzen!). Windows-Tasks: „PolymarketBot" (onlogon) + „PolymarketBot_KeepAlive" (alle 3min, flock verhindert Dup) starten den vbs. Cron-keepalive ENTFERNT (pkill-Konflikt). LEKTION: .sh nach Windows-Edit immer chmod +x ODER via `bash script.sh` aufrufen.

**PERSISTENZ-FIX-alt (2026-06-04, überholt):** WSL wird bei Rechner-Idle/Schlaf suspendiert → Dashboard+cron stoppen (cron kann sich nicht selbst wecken, liegt IN WSL). Lösung: **Windows-Task „PolymarketBot_KeepAlive" alle 10 Min** (`schtasks /sc minute /mo 10`) ruft `wsl.exe -d Ubuntu -- keepalive.sh` → weckt WSL + revived Dashboard von AUSSEN. keepalive nutzt jetzt `python -u` (Logs in dash.log sichtbar). Verbleibendes Gap: bei voller Maschinen-Hibernation läuft nichts (Windows-Tasks auch nicht), aber Wiederbelebung <10min nach Aufwachen. Minor: evtl. kurzes Konsolen-Flackern alle 10min (kann via powershell -WindowStyle Hidden versteckt werden). „Absturz" war nie Code-Fehler — Code startet sauber, nur WSL-Suspend.

**★ HYPERLIQUID PERPS verfügbar (2026-06-04, CLI-Survey):** `experimental list` zeigt `hyperliquid:enabled` + `hyperliquid_trading:enabled` — Perp-Trading voll freigeschaltet! `bullpen hyperliquid` hat: price/mids/markets/orderbook/candles/funding/funding-history/balances/orders/order-status/watch/transfer. **BTC-Perp: Mark $63.7k, 24h-Vol $6.9B, Max 40x, Funding 0.0011%. ECHTE Fees: Taker 0.045%, Maker 0.015%** (vs Polymarket-Binärmärkte). **STRATEGISCH: Reversion-Signal passt VIEL besser auf Perps** — direkter Preis-Bet statt Binärmarkt der die Reversion einpreist; winzige Fees; tiefe Liquidität; eigene HL-Daten (candles/funding/OI). Reconnect mit Binance-Futures-Bot-Idee, aber live tradebar in Bullpen. Caveat: braucht Funds auf Hyperliquid (Arbitrum; aktuell $0, alles auf Polygon/Polymarket), Execution-Layer bauen, Paper-Validierung. HL hat eingebaute `take-profit`/`stop-loss`-Orders. HL-Funds aktuell $0.

**★ PERP-BACKTEST POSITIV (2026-06-04):** `perp_backtest.py` — Reversion als direktionale Perp-Long/Short auf dem PREIS mit echter HL-Taker-Fee 0.045%/Seite, 1h/62 Tage, 6 Coins. **GESAMT ohne Regime-Gate: n=259, Treffer 55%, komp. +38.4%, Sharpe 1.79. Mit Gate: nur +9.4%, Sharpe 0.73** → Regime-Gate SCHADET auf Perps (war nur auf Binärmärkten nötig, von Struktur verzerrt). ERSTES klar positives Ergebnis des Projekts! Dieselbe Reversion war auf Polymarket-Binär break-even, auf Perp profitabel (echter Move + Mini-Fee statt eingepreister Binär-Edge). CAVEATS: nur 62d/ein Regime. → GEHÄRTET (s.u.).

**★★ HÄRTUNG WIDERLEGT DIE THESE (2026-06-04):** `perp_backtest.py` gehärtet — `klines.fetch_history()` (paginiert, ~333 Tage/8000 1h-Bars), +0.03% Slippage/Seite, Monats-Aufschlüsselung. ERGEBNIS: **GESAMT n=3498, Treffer 44.6%, komp. −100%, Sharpe −8.72. Nur 2/12 Monate positiv (Mai+Juni 2026 = exakt das alte 62-Tage-Fenster).** Die +38% waren REINES REGIME-GLÜCK. Über volles Jahr verliert Reversion klar (44.6% < 50% → Crypto-1h-Moves laufen meist WEITER/Momentum, reverten nur in choppy Phasen). Auch das frühere edge_research (60-65%) nutzte nur die letzten 62d → ebenfalls regime-verzerrt. **FAZIT: HL-Integration NICHT bauen — Edge nicht real.** User-Instinkt „erst härten dann bauen" hat vor Bauen-auf-Sand bewahrt. Tiefere Wahrheit: simple Richtungssignale (Kronos, Reversion) schlagen effiziente Crypto-Märkte langfristig nicht. Übrig: Copy-Trading (alongside Winner, eigene Probleme) oder Market-Making (Spread/Rewards statt Richtung, anderer Bot). Andere nützliche Features: `polymarket clob` (direkter Book/fee-rate/rewards, real-time), `watch` (WebSocket real-time für schnellere Signale/TP), `data open-interest`, `rewards --claim`/`clob rewards` (LP/Maker-Rewards).

**Web-Recherche Bias-Strategien (2026-06-04):** Favorite-Longshot-Bias = robustester Edge (Longshots 5-20¢ überteuert, Favoriten 80-95¢ unterbewertet, 2-5%/Kontrakt, behavioral, kein HFT). Krypto-5/15min-Edge = Latenz-Arb + MOMENTUM (Polymarket lagt Binance-Spot, folge dem Spot — nicht Reversion!; bestätigt unseren -Befund, aber braucht sub-100ms = nicht für uns). 4 Bot-Strategien: AMM/Market-Making ($10k, 30s, 0.5-2%/Mon, retail-viable), AI-News-Arb ($2k, <1min), Korrelations-Arb (<500ms), HF-Momentum (<100ms HFT). PolyRadar.pro = nur bezahltes Copy-Tool. 14/20 Top-Trader sind Bots. Quellen: quantpedia, laikalabs, medium (benjamin 5min-crypto, illumination 4-strategies), coindesk.

**Favorite-Longshot-Bias-Studie gebaut (2026-06-04):** `fl_bias.py` (snapshot/resolve/analyze). price-history ALTER Märkte gepurged → Forward-Collection: `discover crypto active` liefert 390 Outcomes/Call aus Preisziel-/Daily-Märkten (spannen Preisbereich 0-1, ideal für Bias). Snapshot erfasst (slug,outcome,preis,end) je erster Snapshot; resolve markiert won via discover-closed (Up/Down via resolver/klines). `analyze` binnt nach Preis-Bucket → reale Gewinnrate vs impliziter Preis + EV nach ~1% Fee, flaggt Edge bei ev>0 & n≥10. Cron alle 10min snapshot+resolve → `fl_data.jsonl`. Märkte lösen täglich auf → Daten über Tage. Check: `python fl_bias.py analyze`. UNSER Preisfilter (0.30-0.70) schließt die FL-Edge-Zone (0.85-0.95) aus — bei Bestätigung umdrehen.

**★ PAPER-BOT AUF FAIR-VALUE-SYSTEM UMGESTELLT (2026-06-04):** `SETTINGS.signal_mode="fairvalue"` (Reversion/Kronos verworfen — keine Edge). `fair_value.opportunities()` (config `FV`): kauft die UNTERBEWERTETE Favoriten-Seite (fair≥0.55, edge≥0.12, Preis 0.30-0.92, Tail-Vorsicht Strike≤50% vom Spot). `runner._fairvalue_scan()` (Branch in scan_once) öffnet diversifizierte Paper-Positionen (Kelly-gesized) auf Preisziel-Märkten. `resolver.outcome_for` erweitert: generische Märkte via `polymarket market`-Detail (Outcome prob≥0.99). take_profit/resolve_due/account_view funktionieren mit Yes/No-Seiten (verifiziert). Erster Lauf: 11 Favoriten-No-Positionen (z.B. „BTC dipt NICHT auf $40k" No@0.705 fair 0.90), $100 deployed. Läuft im Dashboard + cron. analyze.py/daily_report tracken. Implementiert den FL-Bias: überteuerte Longshot-Tails meiden = unterbewertete Favoriten kaufen. NOCH MODELL-WETTE (GBM-Fehler) → fl_bias-Forward-Daten + Paper-Resultate bestätigen über Tage.

**Andere Märkte / FL-Bias auf alle Kategorien (2026-06-04):** `fl_bias.py` `_events` sammelt jetzt über `LENSES=["crypto","sports","all"]` (dedupe) statt nur crypto → 3166 Snapshots (Sport 1944, Krypto 454, sonstig 768). Sport = stärkster FL-Bias + löst schnell auf (Spiele täglich) → Daten wachsen schnell. Wetter-Märkte existieren („highest-temperature-in-<city>-on-<date>", Temperatur-Schwellen, Wetterstation-Auflösung) = idealer Fair-Value-Kandidat via Wetter-API (Open-Meteo/NOAA gratis, Prognosen genauer als Bettor = dokumentierter Edge). Sport-Märkte: 236 via sports-list (NFL/NBA/EPL/MLB...). **Handelbarkeit anderer Märkte: (1) Copy deckt ALLE ab (Whales traden Sport) — läuft schon; (2) FL-Bias misst jetzt alle Kategorien; (3) Wetter-Fair-Value = baubarer nächster Edge (Forecast-API vs Markt); (4) Sport-Fair-Value braucht externe Quoten/Team-Modelle (schwerer), aber FL-Bias greift ohne Modell.** hashdive.com = bezahltes Analytics-Tool (Smart-Score, Screener, Whale-Tracking, RSI/MACD-Charts, deckt auch Kalshi) — read-only, überlappt mit Bullpen-Daten, kein Execution.

**Wetter ins Paper-Trading verdrahtet + Von-Tradern-lernen (2026-06-04):** `weather_value.opportunities()` in `runner._fairvalue_scan` integriert (Krypto+Wetter kombiniert, `ops_all.sort(-edge)` = faire Mischung sonst frisst Krypto das Cash). STATIONS-FIX: CITY-Koords jetzt AIRPORTS/Stationen (London City, Paris-Le Bourget, Tokyo Haneda, Seoul=Incheon küstennah!, NYC=Central Park, HK Observatory...) statt Stadtzentrum, sigma-Floor 1.5. EDGE-KAPPUNG: edge>0.30 = Stationsfehler (z.B. Seoul ≥25C edge +0.86) → nicht traden. Paper läuft jetzt gemischt Krypto+Wetter (2 Wetter-Positionen offen). `learn_trader.py <addr>` analysiert Trade-Historie via `activity`: Markttypen, Seiten, Einstiegspreise, Hold-vs-Sell. **ERKENNTNISSE vom Krypto-Spezialist 0xeccf1ecf (173 Trades): (1) hält meist bis Auflösung (129 BUY/44 SELL) → bestätigt TP-AUS; (2) kauft BILLIG (BUY No/Yes Median 0.16-0.18) = unterbewertete Longshots, NICHT Favoriten 0.30-0.70 wie unser Bot → fordert unsere Favoriten-Strategie heraus; (3) handelt auch 5/15m (49 Trades).** data profile gab $0 (Felder?), activity ist die Goldquelle.

**★ SHANGHAI-STATIONS-FIX (2026-06-06):** User stieß auf einen tief im Minus stehenden Shanghai-„No"-Trade. URSACHE: `weather_value.CITY["shanghai"]` war **(31.198,121.336)=Hongqiao ZSSS** (landeinwärts), aber der Markt löst auf **Shanghai PUDONG ZSPD** auf (küstennah, ~40km östlich, am Meer = kühler) — steht in der Markt-`description` („...recorded at the Shanghai Pudong International Airport Station... Wunderground ZSPD"). ~4°C-Mismatch: Modell-Forecast 29.8°C (Hongqiao) vs Auflösung 26°C (Pudong) → Bot wettete „high ist NICHT 26°C" (No), verlor. FIX: Koords → **(31.143,121.808)=Pudong**, neuer Forecast **25.7°C** (verifiziert, −4.1°C). Juni-6-26C-„No" ($10) war verloren (Bid 0); Juni-7-26C-„No" ($21.84) verkauft bei Bid 0.83 → $21.97 (+$0.13, Break-even raus bevor sie auf 0 läuft); Juni-7-28C-„No" behalten (unter korrigiertem Forecast weiter +EV). **LEKTION: jede Wetter-Stadt-Koords gegen die Markt-`description`/`resolutionSource` prüfen (nennt Station + Wunderground-Link)** — andere Städte (London/Paris/Tokyo/Seoul/Beijing/HK/Taipei/Shenzhen/Guangzhou) sind UNGEPRÜFT und könnten denselben Stations-Fehler haben. Sell via `/api/sell {index}` (holt Bid selbst via marks()); Position per Slug finden → korrekter Index.

**★ WETTER-STATIONS-AUDIT komplett (2026-06-06):** `audit_stations.py` (wiederverwendbar) — entdeckt Wetter-Märkte via `polymarket search "temperature <monat> <tag>"`, holt je Stadt die Markt-`description` und vergleicht die Auflösungsstation gegen `weather_value.CITY`-Koords. ERGEBNIS: **ALLE auditierten Städte korrekt** — beijing=Capital, london=London City, seoul=Incheon, shanghai=Pudong(gefixt), taipei=Songshan, tokyo=Haneda, guangzhou=Baiyun ✅. **Hongkong = Fehlalarm** (Markt löst auf „Hong Kong Observatory" via weather.gov.hk, KEIN Flughafen; unsere Koords 22.302,114.174 SIND die Observatory → korrekt; Regex „recorded at the X Station" verfehlte die andere Formulierung „recorded by the HK Observatory"). NICHT auditierbar (kein aktiver Markt heute): shenzhen (Koords=Bao'an ZGSZ plausibel), nyc (=Central Park, Standard). FAZIT: Nur Shanghai war falsch (Hongqiao→Pudong), Rest stimmt. Re-Audit: `python audit_stations.py`.

**★ GRENZFALL-SCHUTZ Wetter (2026-06-06):** `weather_value.opportunities()` überspringt jetzt „No"-Wetten auf Exakt-Buckets, deren Grad <= `FV.weather_boundary_margin`(1.0°C) von der Prognose entfernt liegt. Grund: Exakt-Bucket = nur ±0.5°C breit → kleiner Prognosefehler kippt „nicht X" zu „doch X" (genau die Shanghai-Falle: fc 25.7 vs Bucket 26, Abstand 0.3). Verifiziert: Shanghai bettet jetzt nur noch 27C/28C „No" (>1°C weg), 26C „No" raus. Offene =26C-„No" ($24.89) verkauft ($24.74, −$0.15). Trifft nur Exakt-Buckets (below/above später erweiterbar). Bucket-Mathematik (`bucket_prob`): exakt =deg -> [deg−0.5, deg+0.5).

**Wetter-Fair-Value gebaut (2026-06-04):** `weather_value.py` — Open-Meteo-Prognose (gratis, kein Key, aus WSL erreichbar) vs. Polymarket-Temperatur-Märkte. Wettermärkte: „highest-temperature-in-<stadt>-on-<datum>" = 11 Grad-Buckets (≤X/=D/≥X), Auflösung per Wetterstation. Existieren heute+morgen (1 Tag voraus), viele Städte (London/Paris/Beijing/Shanghai/Tokyo/Seoul/Guangzhou/Shenzhen/NYC...). `scan()` sucht kommende Tage, holt Prognose-Max-Temp, modelliert Normal(prognose, σ=1.1+0.7·(tage-1)), Bucket-Wahrscheinlichkeit vs Markt. Lief: 77 Buckets, Lücken 0.12-0.26 (Markt erwartet oft andere Temp als Forecast). CAVEATS: (1) **Stations-Mismatch** — meine Koords=Stadtzentrum, Auflösung=spezifische Station (±1-3°C, muss präzisiert werden!); (2) kurzfristige (0.2d) fast aufgelöst; (3) Modell-vs-Markt-Frage. ABER Wetterprognosen treffsicher → robuster als Krypto-GBM, wenn Station stimmt. NICHT in Live-Paper verdrahtet (erst Stations-Fix + Forward-Validierung via fl_bias). **POLITIK: kein sauberes Fair-Value-Modell** (keine Forecast-API wie Wetter; Wahl-Märkte bräuchten Polling-Aggregatoren = event-spezifisch/schwer; viele Politik-Märkte sind modell-lose Binär-Events). Realistischer Politik-Edge = FL-Bias (sammelt jetzt via „all"-Lens) + Copy politischer Whales.

**Fair-Value-Detektor mit Kerzen (2026-06-04):** `fair_value.py` — für Krypto-Preisziel-Märkte (220 aktiv via discover crypto): parst Typ (reach=Up-Touch / dip=Down-Touch / above=Terminal) + Strike + Datum aus der Frage, berechnet faire Wahrscheinlichkeit aus Binance-Vola (30d realized, annualisiert) + Restzeit (GBM + Reflektionsprinzip für Touch), vergleicht mit Marktpreis. Liquiditätsfilter (volume≥20k, kein 0.50-Default). FUND: **Markt bepreist TAIL-Events (dips/moons) systematisch HÖHER als Modell** (z.B. BTC-dip-40k Markt 0.29 vs fair 0.09) = Favorite-Longshot-Signatur. CAVEAT: GBM unterschätzt Krypto-Fat-Tails → Teil der Lücke = Modellfehler, nicht Bias. `fl_bias.py` (Forward-Outcomes) trennt beides über Zeit. „Bot yes/no": aktuell Reversion (3h-Move-Gegenrichtung, p_up~0.62, Regime-Gate, edge.evaluate vs Markt-ask 0.30-0.70) — laut Backtest ohne Edge. Krypto-Abdeckung jetzt umfassend: 5/15m+daily updown, 220 Preisziel-Märkte (fair_value + fl_bias).

**Reversion-Reichweite ehrlich:** Signal ist kurzfristig-direktional → passt NUR auf 5m/15m updown. Daily (24h) + Preisziele = anderer Markttyp, brauchen andere Signale. „Alles Krypto traden" mit 1 Signal nicht möglich. 10% Fee macht gerichtete Strategien grundsätzlich schwer → Market-Making (LP-Rewards kassieren statt Fee zahlen) ist der einzige fee-positive Pfad für den breiteren Krypto-Raum.

**Copy reaktiviert (2026-06-04):** 2 aktivste Trader (nach volume_1d, keine Bots, aktiv heute): `0xbddf61af533ff524d27154e589d2d7a81510c684` ($7.9M/30d, vol $332k) + `0xb6d6e99d3bfe055874a04279f659f009fd57be17` ($908k/30d, vol $101k). FEHLER vermieden: zweite Adresse war zuerst erfunden (Skript zeigte nur [:14]) → korrigiert. `data leaderboard --sort volume` timeoutet serverseitig → über pnl/copyability/winrate-Pool nach volume_1d ranken. Guthaben auf $42.55 erholt. Modus auto, exit mirror_sells, Caps je Sub: $10/Trade, Budget $25, Tageslimit $20 (kombiniert ~$50 Risiko). **WICHTIG: Polymarket-Mindestgröße ~$10/Trade** (User wollte $3 → auf $10 angehoben). Läuft serverseitig (unabhängig vom lokalen Auth-Token, der nach ~15 Min abläuft). Steuern: `bullpen tracker copy list|status|executions|pause|stop|risk`. Hinweis bei Start: externe Wallets „may not execute" (generische Warnung).

**★ COPY-HEALTH-FILTER + Copy-P&L im Dashboard (2026-06-05):** Dashboard hat neue Sektion **„Echte Copy-Positionen (offen, mit P&L)"** — zieht `polymarket positions` (current_value>0), zeigt Markt/Seite/Investiert/Wert/P&L/P&L% + Summenzeile wie Paper-Trading. `fetch_copy_live()` erweitert (positions/pos_pnl/pos_invested/pos_value). Die alte Execution-Liste ist nur ein Event-Log (zeigt sofort „Completed" = platziert, KEIN P&L) → umbenannt „Kopierte Trades (Log)". User-Beschwerde „hängt + keine PnL" war das: Log ≠ Positionsergebnis. **`copy_health.py` (neu):** bewertet aktive Copy-Subs, stoppt schwache. Stopp-Gründe: pnl_30d<0, WR_30d<Schwelle (`COPY.health_min_winrate=0.55`), oder „verliert nur bei uns" (≥3 Copies & P&L<0). `python copy_health.py` prüfen, `--stop` anwenden, `--min-wr 60` override. Dedup CASE-INSENSITIV (Adressen kamen in Checksum- UND Kleinschreibung → Doppel-Sub). **KERNERKENNTNIS: „nur Verlust" ist als Filter wertlos — JEDER Whale ist netto im Plus (sonst kein Whale). Bei festem $10-Mirror zählt die TREFFERQUOTE.** Roster auf **4 Trader ≥55% WR** gestrafft: BEHALTEN 0xbddf61af(55%), 0xf1528f12(57%), 0xeccf1ecf(75%), 0x91e8a6ed(65%); GESTOPPT 0x0dcf5b(40%, war der EINZIGE der handelte + kostete −$1.65), 0xb6d6e99d(43%, war doppelt), 0xe47c17e9(54%). Zusatz-Befund: wir überspringen **6/10 Trades** des Whales (Budget/Size-Caps) → schiefe Teilmenge = Hauptgrund schwacher Copy-Performance. **Stoppen verkauft NICHT bestehende Positionen** (mirror_sells = nur künftige Sells) → die 2 Verlust-Positionen (BTC-Daily −78%, Dota2) lösen sich selbst auf. Lücke: copy_health winkt „keine Daten" durch (ein 43%-Trader fiel zeitweise aus dem Leaderboard → musste explizit per exakter API-Adresse gestoppt werden, nie Adresse raten).

**★ STÜNDLICHER COPY-AUTO-REFRESH + Paper $150 + Hyperliquid-Fund-Befund (2026-06-05):** `copy_refresh.py` (neu) aktualisiert das Copy-Roster **stündlich selbst** (Cron `0 * * * *`, in crontab.txt) → **kein manuelles An/Aus mehr nötig** (User-Wunsch). Wählt die HEUTE aktivsten Top-Trader via `copy_trader.rank_today(fetch_pool("1d"))` (hot today: pnl_1d≥1000, wr_1d≥0.55, pnl_30d≥0, aktiv heute, keine Bots) + Qualitätsbar `wr_30d≥COPY.health_min_winrate(0.55)`, Top-N=3 nach pnl_1d. **Reconcile:** stoppt aktive Trader die rausgefallen sind, startet neu qualifizierte, lässt die Schnittmenge unberührt. GUARDS: `tracker copy list` nicht lesbar (Auth) ODER kein Ziel-Trader → Roster UNVERÄNDERT (nie blind leeren). Loggt nach `copy_refresh.log`. Erste Rotation (2026-06-05): 4→2 aktiv, behielt 0xf1528f12 (heute +$81k, WR 75%), startete 0xd38ad2003783 (+$77k, WR 71%, WR30d 56%), stoppte 0xbddf61af/0xeccf1ecf/0x91e8a6ed (heute nicht hot). **WICHTIG: Roster ist jetzt DYNAMISCH** — wer heute nicht handelt fliegt raus, kommt automatisch zurück wenn wieder aktiv. **Paper-Kapital auf $150** (`SETTINGS.start_capital=150`, paper_state start=150 + cash so gesetzt dass cash+investiert=150, also +$50 frisches Cash, 11 offene Positionen behalten). **HYPERLIQUID-FUND-BEFUND:** `funds balances` zeigt Polymarket nur **$9.33 pUSD** (deshalb Mini-Copies wie der $2-BTC-Trade!), aber **$20.86 USDC auf Hyperliquid SPOT** (Perp-Konto = $0, nicht „perp"). **CLI KANN NICHT cross-chain transferieren** — `hyperliquid transfer` macht nur spot↔perp INNERHALB Hyperliquid; `funds --help` sagt explizit „For deposits and cross-chain transfers, use https://app.bullpen.fi". → Hyperliquid(Arbitrum)→Polymarket(Polygon) muss der User in der **Web-App app.bullpen.fi** machen (kein CLI-Pfad, und Geldbewegung gehört eh in die UI). **BTC-Minus-Trade** (bitcoin-up-or-down-on-june-5-2026 „Up", −78%): löst 2026-06-05 16:00 UTC auf (Daily Noon-ET), „Up" nur noch 8.5% bepreist → Trader kann theoretisch recht behalten, aber Verkaufen(~$0.44)≈Halten-EV, nur $2 Einsatz → einfach auslaufen lassen. Stoppen verkauft Position NICHT.

**★ ECHTER GRUND SCHWACHER COPIES gefunden + gefixt (2026-06-05):** Skip-Gründe aus `tracker copy executions` (Feld `skip_reason`): **„Trade size $227/$5201/$100 exceeds maximum $10.00"** — die LAUFENDEN Subs trugen noch `max_trade_size=$10` (Config-Fix auf 1 Mio greift nur bei NEUSTART, nicht rückwirkend!) → wir kopierten nur die Mini-/Low-Conviction-Trades der Whales und überspringen alle echten großen → schiefe Teilmenge = schwache Performance. Plus **„Total budget reached ($30 of $25)"** = Budget-Cap zu klein. `tracker copy edit` kann NUR `--amount/--percentage/--balance-percentage` ändern, NICHT die Caps → Subs müssen für Cap-Änderung **gestoppt + neu gestartet** werden. FIX: `COPY.budget 25→50`, `daily_limit 20→30` (max_trade_size schon 1 Mio), Roster gestoppt+neu aufgebaut über copy_refresh → neue Subs tragen korrekte Caps. Filter zusätzlich gelockert: `today_min_pnl 1000→200`, `top_n 3→5` → 3 Quali-Trader (0xf1528f12 Sports, 0xd38ad20, 0x10fc6c Politics WR73%). **ABER der bindende Engpass bleibt das Polymarket-Guthaben $9.33** — Copies brauchen $10 Min, also weiter „insufficient"-Skips bis der User die $20.86 von Hyperliquid via app.bullpen.fi rüberbridgt. Edit-Optionen `--percentage`(% der Trader-Größe) / `--balance-percentage`(% unseres Guthabens) wären conviction-gewichtet, aber bei $9 Guthaben unsicher → fixe `--amount $10` bleibt.

**★ COPY-TRADE-ANALYSE-Tool + erstes echtes Ergebnis (2026-06-05):** `copy_analyze.py` (wie analyze.py fürs Paper) — Executions nach Status/Gründen, Completed-Copies klassifiziert (OFFEN/GEWONNEN/VERLOREN via Position-Wert + Markt-closed), je Trader, Netto realisiert+unrealisiert, Guthaben-Kapazitätswarnung. **ERSTES ECHTES COPY-ERGEBNIS:** 3 ausgeführte Copies — CS2 Liquid −$10 (verloren), MLB Cubs −$3.47 (offen, fällt), Dota LGD **+$8.87 (gewonnen!** $10→$18.87, der $5-Stop löste korrekt NIE aus, „laufen lassen" statt „bei $7.36 verkaufen" zahlte sich aus). Plus BTC Up −$2 (älter, verloren, User-OK). **Netto ≈ −$4.60 bis −$6.60.** EFFIZIENZ KATASTROPHAL: 20 Executions → nur 3 ausgeführt (15%), **9 Failed (8× „Insufficient balance"** bei $8.75 Guthaben!), 5 Skipped (3× altes $25-Budget, 2× Preis >95¢), 3 Pending (Tennis, scheitern auch). **2 KERNBEFUNDE: (1) Kapital ist die harte Wand** — bei $8.75 scheitert fast jede neue Copy; Copy-Trading praktisch tot bis Transfer. **(2) Das sind Münzwurf-Sportwetten (0.53-0.62 Einstieg = ~Coinflips), keine Edge** — über 3-4 Copies reine Varianz, ein CS2-Totalverlust fraß den Dota-Gewinn. Bestätigt erneut: Copy von Sport/Esports-Whales mit Mini-Festbetrag = Glücksspiel. **SOFORT-HEBEL: Dota-Gewinn einlösen** (redeem=False, settling → bald True) → +$18.87 Cash → Guthaben ~$27 → Copies gehen wieder + Gewinn gesichert.

**★ DASHBOARD-COPY-FIXES + FLAKY-POSITIONS-API (2026-06-05):** User „neueste Copytrades fehlen". URSACHE 1: `fetch_copy_live` nutzte FALSCHE Execution-Feldnamen (`market_slug`/`followed_address`/`timestamp`) — echte heißen `source_market_slug`/`source_trader_address`/`detected_at`/`copy_amount_usd`/`market_title` → Log-Zeilen blank. FIX: echte Felder + neueste-zuerst-Sort + Markt-TITEL statt Slug. URSACHE 2: `polymarket positions` (Bullpen-Source) ist **laggy + inkonsistent** — frische Copy-Fills brauchen 1-2 Min bis sie in der Liste auftauchen (WNBA-Copies fehlten kurz, dann da); SETTLED Positionen verlassen die AKTIVE Liste (Dota verschwand nach Spielende) → nur via `--redeemable`/`--closed` sichtbar; verschiedene Calls liefern verschiedene Subsets. Auch Copy-Positions-P&L: `unrealized_pnl` ist bei aufgelösten 0 → jetzt `pnl = value - invested`. **FOLGE-BUGS im Wächter gefixt:** (a) Auto-Redeem prüfte die AKTIVE Liste auf redeemable → verpasst settled Gewinne die die Liste verlassen haben → jetzt `polymarket positions --redeemable`; (b) check_stops entfernte einen Stop sobald die Position „fehlt" → bei API-Flackern fälschlich → jetzt nur entfernen wenn `market closed=True` (das leerte real_stops.json beim Dota-Stop, war KEIN User/Linter). Dashboard hat field-name+pnl-Fix (verifiziert: mlb + 2 WNBA mit P&L). LEHRE: der Bullpen-Positions-/Balance-Daten sind träge — über mehrere Sekunden/Calls inkonsistent, nicht für Echtzeit-Entscheidungen in <1min verlassen. **UNIVERSELLER $5-COPY-STOP (2026-06-05):** `real_stop_watch.check_copy_stops()` — verkauft JEDE Copy-Position (Markt-Slug aus `tracker copy executions` source_market_slug, prefix-match wg. Spread-Suffix) bei Wert <= `COPY.copy_stop_value` ($5, 0=aus). Guard: nur wenn Markt NICHT closed (settling/aufgelöst -> redeem statt sell). $5 auf $10-Einsatz = 50%-„Disaster-Floor": lässt Schwankungen zu (Dota dippte auf $7.36 und gewann -> hätte NICHT ausgelöst), kappt aber Totalverluste. Verifiziert: erkennt mlb/wnba/wnba-spread als Copy, hält da alle >$5.

**Echte-Positions-Stop-Wächter (2026-06-05):** `real_stop_watch.py` + `real_stops.json` — Cron jede Minute (`* * * * *`), zwei Funktionen: **(1) AUTO-REDEEM** — löst JEDE gewonnene (redeemable) Position automatisch ein (`polymarket redeem --condition-ids <cid>`) → Cash zurück (reine Upside, immer an, nicht konfig-abhängig); **(2) STOP-LOSS** — verkauft eine Position (`polymarket sell <slug> "<outcome>" --max --yes`) sobald `current_value <= stop_value`. redeem-Syntax: `--condition-ids` komma-separiert, kein --yes nötig. Auto-Redeem für Dota-Gewinn eingerichtet (User „nur a"): sobald Dota settled (redeem False→True), nächste Minute +$18.87 ins Cash → Guthaben ~$27 → Copies gehen wieder. SICHER: fasst NUR in real_stops.json gelistete Slugs an; entfernt Eintrag nach Verkauf/Auflösung (one-shot, self-cleanup). Wiederverwendbar für jeden manuellen Stop. Verkaufs-Syntax: SHARES sind Stück nicht USD, `--max` = ganze Position. `tracker copy edit` kann übrigens nur amount/percentage/balance-percentage, nicht die Caps. Erster Einsatz: Dota2-Copy-Position (LGD Gaming, orphan nach Stopp von 0x0dcf5b) Stop bei $5 — User-Wunsch „bei rund 5$ beenden" (Wert war $9.91→$7.36 fallend, Match löst 18:00 UTC auf). Caveat: bei voller PC-Hibernation pausiert Cron (Lücke).

**★ BIAS-WIEDERKEHR-VERFOLGUNG (2026-06-06):** `fl_bias.recurrence_data(min_n=10)` + `recurrence()`-Printer + CLI `python fl_bias.py recur`: gruppiert aufgelöste Snapshots nach `ts`-Tag, zeigt je Bucket die Tages-EV → trennt KONSISTENTE Edge (jeden Tag +EV) von Eintagsfliegen. ERGEBNIS (3 Tage): **0.85-0.95 = 3/3 +EV konsistent (echte Edge), steigend** (+0.006/+0.043/+0.080); 0.00-0.15 + 0.45-0.55 = 0/3 (konsistente Fallen); **0.30-0.45 (User-Fund) nur 2/3 — am 06-04 −0.077 → NICHT konsistent**, die kumulierte Stern-Anzeige war irreführend. Ins Dashboard integriert (manage_loop setzt `LATEST["fl_recur"]`, HTML #flrecur + JS-Grid mit pos/neg-Färbung + ✅konsistent/🔴Falle). **★ LEKTION (Debugging-Falle): `/api/state` gibt eine FESTE Key-Auswahl zurück (`json.dumps({"scan":..,"fl":..,...})`) — NEUE LATEST-Keys MÜSSEN dort ergänzt werden, sonst im Browser unsichtbar obwohl gesetzt!** Stundenlange Prozess-/Lock-Jagd war unnötig (Code lief korrekt). ZWEITE LEKTION: `pkill -9 -f dashboard.py` killt die EIGENE Shell (deren Kommando enthält „dashboard.py") → Klammer-Trick `[d]ashboard.py` ODER kill per PID via `fuser /tmp/run_dashboard.lock`. run_dashboard.sh nutzt flock auf /tmp/run_dashboard.lock (nur 1 Loop); Lock-Halter = `fuser` zeigt loop+dashboard-PIDs.

**★ LIVE-HANDEL-SETUP (2026-06-06):** User: Copy-Trading AUS, Bot soll selbst live traden. COPY AUS: alle Subs gestoppt + `copy_refresh`-Cron auskommentiert (sonst holt er stündlich Trader zurück). Guthaben auf **$34.26** erholt. **`live_scan.py` gebaut** — Fair-Value-FAVORITEN (style favorit, Preis>=0.50) live via `polymarket buy <slug> <outcome> <amount_usd> --yes --max-price X`; DRY default (`--preview`, kein Geld), `--execute` = echt, `--include-2of3` = Fallback-Zone (Longshots 0.30-0.49, die 2/3-Bias-Buckets) falls Favoriten leer. Caps: $7/Trade, max $28 deployed, max 4 neu/Lauf, 5% Slippage; nutzt opportunities()-Guardrails (Edge, Grenzfall-Schutz). LONGSHOTS bewusst RAUS (Paper-„6/6" waren ALLE Verkäufe = Cherry-Picking-Illusion; Beweis: Yes UND No auf demselben ETH-dip-1500 beide „gewonnen" durch Preis-Schwankung; Bias sagt 0-0.15 = Falle). Dry-Run fand 3 HK-Wetter-„No"-Favoriten (~$21, Preis 0.68-0.76 = 0.70-0.85-Zone, HK-Station verifiziert). **HAND-VERKAUF LIVE gebaut:** Dashboard `/api/sell_live` (POST slug+outcome -> `polymarket sell --max --yes`) + „verkaufen"-Button je Zeile in „Echte Positionen (live)"-Tabelle (fullslug-Feld dafür ergänzt, da Anzeige-Slug auf 38 Zeichen gekürzt). STAND: wartet auf User-GO für `live_scan.py --execute` (erste echte Order). **★ ERSTE LIVE-TRADES AUSGEFÜHRT (2026-06-06):** User: „mehr Risiko, Paper-Modus so wie er ist live, $35 max Verlust ok" -> live_scan auf VOLL-PAPER (alle Styles, $6/Trade, max $32 deployed, max 6) + `--execute`. **4 echte Wetter-„No"-Käufe** (3×HK, 1×Seoul, je ~$6.13) bestätigt via `polymarket activity --type trade`; Guthaben $34.26->$9.74 (−$24.52). BTC-70k-Longshot korrekt abgelehnt (Fill 0.18 > max-price 0.173 = Slippage-Schutz griff, kein Geld). buy-Antwort hat `{"preview":{estimated_shares,...}}`-Format trotz --yes -> Ausführung NUR via Guthaben/activity verifizierbar, nicht aus der Antwort. **PROBLEM: `polymarket positions` zeigt die 4 frischen Positionen auch nach Minuten NICHT** (laggy Bullpen-API, schon bekannt) -> Dashboard + Hand-Verkauf-Button listen sie erst nach Sync. Existieren aber (Shares gehalten). CLI-Verkauf via Slug+Outcome ginge sofort.

**★★ DER POSITIONS-LAG-FIX (2026-06-06) — `--source polymarket`!** Die ganze Session kämpften wir mit `polymarket positions`, das frische Positionen minutenlang NICHT zeigt (Dota verschwand, WNBA-Lag, Live-Trades unsichtbar trotz polymarket.com). URSACHE GEFUNDEN: `polymarket positions` hat `--source` — Default `bullpen` (träger Cache!) vs **`polymarket` (Polymarkets echte Daten, sofort aktuell)**. Test: bullpen 43 Pos/0 live, **polymarket 4 Pos/4 live** ✅. `--source polymarket` hat ALLE Felder (slug/outcome/current_value/invested_usd/avg_price/shares/condition_id/unrealized_pnl). UMGESTELLT: `dashboard.fetch_copy_live`, `live_scan.held()`, `real_stop_watch.main()` nutzen jetzt `--source polymarket`. (Andere Tools copy_analyze/copy_health/audit könnten auch umgestellt werden.) **DASHBOARD-SLOWNESS-FIX (gleicher Tag):** `/api/state` brauchte 8.6s weil `account_view()` 16 Paper-Positionen synchron markierte -> jetzt im `manage_loop` gecached (`LATEST["account"]`), `/api/state` liefert `LATEST.get("account")` -> **0.01s**. 4 Live-Wetter-Positionen ($24, P&L −$0.15) erscheinen jetzt im Dashboard mit Hand-Verkauf-Buttons (fullslug vorhanden).

**★ DASHBOARD „LEER BEI ALLEN"-BUG gefixt (2026-06-06):** Nach Neustarts (bevor der 300s-Scan lief) war LATEST["scan"]={} (leer). JS: `let opps=s.scan?s.scan.opportunities:[]` — **leeres {} ist in JS TRUTHY** -> `s.scan.opportunities`=undefined -> `undefined.forEach()` wirft -> ganzes `load()`-Rendering bricht ab -> ALLE Sektionen leer. FIX: `(s.scan&&s.scan.opportunities)||[]`. Zusätzlich `load()` jetzt in try/catch (zeigt künftige Render-Fehler in #err statt alles zu blanken). Diagnose: /api/state-Dump zeigte `scan keys: []` = leeres dict. LEKTION: in JS `obj?obj.x:[]` schützt NICHT vor leerem {} (truthy) -> immer `(obj&&obj.x)||[]`. **ZWEITER Bug (gleicher Tag, dieselbe Ursache „leer"):** `PAGE = """...JS..."""` ist ein NICHT-raw Python-String -> mein `sellLive`-confirm `'...?\n'` wurde zu einem ECHTEN Zeilenumbruch serviert (unterminierter JS-String = Syntaxfehler = ganzes <script> parst nicht = load() undefiniert = alles leer, try/catch greift NICHT weil Skript gar nicht lädt). ZUSÄTZLICH wurde die fetch-URL als `'\api\sell_live'` (Backslashes!) gespeichert. Gefixt via _fix.py (Umbruch->Leerzeichen, Backslashes->Forwardslashes). **LEKTION: im nicht-raw PAGE-String KEINE `\n`/`\x` in JS-Strings (Python interpretiert sie). Edit-Tool-Escaping bei Backslashes ist tückisch -> Datei-Fixes lieber per Python-Skript (open/replace/write).**

**Dashboard (2026-06-04):** `~/polymarket-bot/dashboard.py` — Python-stdlib (kein Flask, war nicht im venv), Port 8787. **WICHTIG: bind auf `0.0.0.0` (NICHT 127.0.0.1!)** — sonst von Windows-Browser NICHT erreichbar (127.0.0.1 ist WSL-intern). „Dashboard geht nicht" = war auf 127.0.0.1 gebunden → Fix: `ThreadingHTTPServer(("0.0.0.0", PORT)...)`. Zugriff Windows: http://localhost:8787 (oder WSL-IP:8787). Background-Thread scannt alle 300s (`scan_loop`), zeigt Opportunities (alle TFs), offene Positionen mit Live-Mark (best_bid) + Verkaufen-Button, kopierbare Top-Trader. Routes: `/`, `/api/state`, POST `/api/scan`, POST `/api/sell` {index}. **Zusätzlicher 60s-`copy_loop`** zeigt LIVE: echtes pUSD-Guthaben (`funds balances`), aktive Copy-Subscriptions (`tracker copy list`) und kopierte Trades (`tracker copy executions`) — alles im Dashboard neben den Paper-Signalen. Auth-Fehler werden gnädig angezeigt (nicht crash). Scan-Kernlogik in `runner.scan_once()`/`evaluate_tf()` (von CLI + Dashboard genutzt). `paper_engine.sell(index, price)` schließt Position zum Bid, PnL ins Cash. Getestet: Sell-Pfad funktioniert.

**Binance/Bybit-Bot-Integration: NOCH KEINE.** `trading-bot/` (Windows, EXCHANGE umschaltbar binance|bybit via ccxt, kein separater Bybit-Bot) hat KEINEN Einfluss auf Polymarket-Entscheidungen — Binance dient nur als Kline-Datenquelle für Kronos. Strategie-Teile (momentum, funding_carry, perp_edge, auto_universe) könnten als Ensemble-Signal neben Kronos einfließen — TODO. Siehe [[trading-bot-project]].

**Scheinedge gefixt (2026-06-04):** Ursache war falsche Referenz. Up/Down-Daily-Markt resolved Noon-ET→Noon-ET: „Up" wenn Binance BTC/USDT 1m-Close am endDate höher als 24h davor. Echte Referenz = `price_at(endDate-24h)`, Horizont = now→endDate (beides aus `polymarket market <slug>` JSON: Feld `endDate`). `kronos_signal.p_up_anchored(df, reference, horizon)` vergleicht prognostizierten End-Close gegen echte Referenz. Ergebnis: P(up) stimmt jetzt mit Markt überein (vorher 0.65 vs 0.06 Scheinedge → jetzt 0.00 vs 0.057).

**Intraday-Anchoring + TZ-Bug gefixt (2026-06-04):** Intraday-Regel: „Up" wenn BTC-Preis am Fensterende ≥ Fensterstart. `runner._kronos_anchored()` ankert intraday gegen `price_at(slug_ts)` (Fensterstart), Horizont = now→Fensterende in 1m-Bars. **Wichtiger TZ-Bug gefunden:** `naive.timestamp()` nahm lokale TZ (WSL=Europe/Berlin, UTC+2) statt UTC → 2h Versatz in Slugs UND Daily-Referenz. Fix: `calendar.timegm()` in `polymarket.align_unix` + `klines.price_at`. ACHTUNG: Intraday-Märkte zeigen oft `acceptingOrders=False` → aktuell nicht tradebar, Signal aber korrekt.

**Binance-Futures-Ensemble gebaut (2026-06-04):** `futures_signal.py` spiegelt trading-bot-Logik (Kaufman ER + Momentum-Lookback + Trend-EMA50 + Funding via fapi). `signal()` → dir/p_up/er/mom/funding; `combine(p_kronos, fut)` → Agreement-Gate (nur traden wenn Kronos+Futures gleiche Richtung, `FUT.require_agreement`) + gewichteter Blend (`FUT.kronos_weight=0.6`). Bei Futures-neutral wird Kronos Richtung 0.5 gedämpft → reduziert überkonfidente Kronos-Trades. In `runner.evaluate_tf` verdrahtet; Dashboard zeigt Kronos→Ensemble + Fut-Richtung. Config: `FUT` in config.py.

**Kapital-Modell (User-Vorgabe):** Paper-Trading zuerst, virtuelles Konto startet $100. Equity = Cash + Σ(offene Positionen zu aktuellem Mark-Preis); Sizing gegen aktuelle Equity, nicht fixe $100.

**Auto-Resolver + Duplikat-Schutz (2026-06-04):** Paper-Positionen „hingen" (nie aufgelöst, Cash auf $0 verbrannt durch Über-Handel: 10 Positionen im selben 15m-Fenster). `resolver.py` (`resolve_due()`, `outcome_for(slug)`) löst beendete Up/Down-Fenster anhand ECHTER Binance-Preise auf (Fensterende vs Start/Referenz), +60s Puffer; in `dashboard.do_scan()` eingebunden. `scan_once` hat jetzt Duplikat-Schutz (kein 2. Trade auf offenen Slug, `open_slugs`-Set). **TZ-Bug erneut:** `naive.timestamp()` nahm lokale TZ → in resolver `time.time()` (echter UTC-Epoch) statt `now.timestamp()`. Generell: nie `naive_utc.timestamp()` auf diesem System (Europe/Berlin) — `calendar.timegm()` oder `time.time()`.

**Reihenfolge (vereinbart):** 1) Wallet-Fix async anstoßen (blockiert Paper nicht) → 2) Märkte read-only erkunden → 3) Kronos lauffähig machen → 4) Edge-Layer + Paper-Engine. **Alle 4 erledigt (2026-06-04).**

**Engine gebaut & getestet** in WSL `~/polymarket-bot/`: `config.py`, `klines.py` (Binance live), `kronos_signal.py` (P(up), mini/base gecached, Modell-Cache), `polymarket.py` (Slug-Bau + `bullpen price`), `edge.py` (EV + fractional Kelly, fee/spread-bewusst), `paper_engine.py` ($100-Konto, Equity=Cash+MTM, JSON-State), `runner.py` (scan/status/resolve). Start: `~/kronos-venv/bin/python runner.py scan --tf daily 15m --model mini`. Voller Zyklus open→MTM→resolve verifiziert.

**★★ KRITISCHE KORREKTUR (2026-06-04): Die 10% Fee war ein PHANTOM.** `fee_bps:1000` im Markt-Metadatensatz wird NICHT berechnet. `bullpen polymarket preview <slug> <outcome> <amount>` zeigt die ECHTE Gebühr: `trading_fee` $0.01-0.05 auf $10 (~0.1-0.5%), `fee_status:zero/estimated`. Bestätigt: 2 echte Copy-Trades deployten exakt $10 (nicht $11). GILT FÜR ALLE MÄRKTE (Sport/Politik/Krypto alle fee_bps 1000 aber real ~0). → `SETTINGS.fee_bps` von 1000 auf **100 (1% konservativ)** gesetzt, `edge.evaluate(fee_bps=SETTINGS.fee_bps)` in beiden runner-Calls, fee_pct-Anzeige korrigiert. **FOLGE: Breakeven sinkt 52.6%→~50%, Reversion-Edge (60-65%) wird tragfähig.** Die ganze frühere „keine Edge"-Analyse war durch die Phantom-10% verzerrt! Alte Notiz unten (10% Fee) ist überholt.

**Kritische Befunde (TEILS ÜBERHOLT, s.o.):** (1) ~~Up/Down-Märkte haben 10% Fee~~ → real ~0%. (2) Binance-Klines aus WSL erreichbar. (3) **Signalqualität UNVALIDIERT**: Daily-Kronos-Forecast sieht laufende Tageskerze nicht → erzeugte Scheinedge (P(up)=0.65 vs Markt 0.07). Nächster echter Schritt vor Echtgeld: **Backtest + Kalibrierung von P(up)** (run_backtest_kronos.py in ~/Kronos/examples als Basis), höher aufgelöste Live-Klines, Modellvergleich mini vs base.
