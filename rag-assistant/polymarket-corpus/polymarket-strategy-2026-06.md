---
name: polymarket-strategy-2026-06
description: "Polymarket Up/Down Strategie-Stand Juni 2026 — Richtung ist Münzwurf, Edge = cheap-hold Fehlbepreisung; laufende Paper-Experimente + Live-FOK-Realität"
metadata: 
  node_type: memory
  type: project
  originSessionId: 588a28a4-2c22-4acf-8911-2520f5d6c108
---

Stand 2026-06-11 des Polymarket-Bots (Bausteine siehe [[polymarket-bot-project]]). Kern-Erkenntnis nach vielen Analysen dieser Session:

**Richtung ist ein Münzwurf.** 5m/15m Up/Down-Richtung über 7242 Logfenster (`ml_features.jsonl`): LogReg-AUC = 0.500, 3× unabhängig bestätigt (Replay 51.7%, ML 0.50, jetzt 0.500). Kein Feature sagt die Richtung vorher — btc_cvd schrumpfte mit mehr Daten 0.531→0.516 = Rauschen. **Die Edge liegt NICHT in Vorhersage, sondern in Mechanik + Fehlbepreisung.**

**cheap-hold getestet & VERWORFEN (2026-06-11):** Hypothese: die billige Seite (Einstieg 0.30-0.40) ist unterbewertet (FLB: gewinnt real ~49% bei ~38% Preis), also halten bis Auflösung. Sim versprach +446$/+33%/Trade. **Vorwärts aber nur +4$ / Break-even** (43% WR) — die wenigen `tp_cheap`-Großgewinner werden von den vielen auf 0 auflösenden Verlierern gefressen. → wieder normales TP für alle. Lehre: die Fehlbepreisung ist real, aber zu schwach, um das Halten der Verlierer zu bezahlen. **Die echte Up/Down-Edge ist also nur: TP-Mechanik + früher Einstieg + US-Handelszeiten.**

**Laufende Paper-Experimente (getaggt):** (1) SOL-5m-Signal-Inversion (+57, AUC 0.468); (2) Volatility-Hunter (`volhunter.py`, $100 Paper, Dashboard 🌋, lauert noch). 15m = BTC+SOL, 1h = BTC+ETH (auch Live). **Verworfen (vorwärts ohne Edge):** btc_cvd-Gate, Spät-Einstieg, cheap-hold. **★ Zeitfilter (2026-06-11):** EU-Vormittag 07-12 UTC verliert (−139, WR~56 %), US 13-21 gewinnt (+252, 73 %), Asia/Spät solide → `SKIP_HOURS_UTC={7..12}` in `updown_paper.py`, keine neuen Up/Down-Einstiege in dem Fenster (Paper+Live, TP/Resolve laufen weiter). **Fair-Value 35%-Cap:** `runner._fairvalue_scan` setzt nur 35 % des Kapitals ein (`max_deploy=0.35*(cash+deployed)`), Rest bleibt Cash.

**Live-Realität (FOK/dünne Bücher):** Live Up/Down füllt schlecht — bullpen `buy` ist Fill-or-Kill, die dünnen kurzen Bücher killen selbst $3-Orders. `SLIPPAGE 1.20` hilft (handelte 2026-06-11, +38% TP), aber sporadisch. **Paper überzeichnet Live** (Illusion unendlicher Liquidität — wichtig vor Echtgeld). Live cap $10/$3, meist AUS (Dashboard-Schalter). 1h aus Live raus (nur 5m/15m); Live spiegelt cheap-hold + späte-15m.

**Wetter KOMPLETT raus (2026-06-11):** Wetter-Paper fiel +71 → +26 → **−97** (Verlustserie, doch zu varianzreich) → raus aus **Live** (`live_scan.py` MAX_WEATHER=0 + weather_value aus ops) UND **Paper-Fair-Value** (`runner._fairvalue_scan`: weather_value auskommentiert). Fair-Value = nur noch Krypto (+373, der Motor). Auch **ETH-15m raus** (−46/57% WR), 15m = BTC+SOL.

**★ SPÄT-EINSTIEG WIDERLEGT (2026-06-11):** Hypothese „spät einsteigen → billige Seite → cheap-hold" getestet (15m 8-5min, 1h 45-5min) und per `entry_remf`-Analyse WIDERLEGT: 15m FRÜH (remf≥0.80) = +150 / 74% WR, spät/mittig tiefrot (0.60-0.80 = −69). Gilt über 5m+15m+1h: **früher/frischer Einstieg IST die Edge**, spät verliert auf Richtung (frisst die Cheap-Unterbewertung auf). Beide auf früh zurückgestellt (`remain_frac`). Mid-5m muss nicht mehr getestet werden — gleiche Logik. (cheap-hold wurde danach ganz entfernt, s.o. — vorwärts nur Break-even.)

**★ Update 2026-06-13 (ersetzt die Live-Config-Aussagen oben):** **Live = NUR 5m** (15m+1h raus aus Live; Paper behält alle TFs für Daten). 5m trägt klar (+3,7…6,6% ROI/Combo, BTC/SOL/ETH/BNB), 15m dünn-bis-negativ + hohe Varianz (der „flache" Paper-Mischwert kommt von 15m, NICHT von 5m — 5m machte 12./13.06. +65/+29 bei WR 76–80%), 1h ~neutral. **Einstiegsdecke 5m <0,50** (`FIVE_M_ASK_MAX=0.50`): <0,50 = +8,5% ROI vs 0,50–0,55 nur +1,4%; resolved-Verluste ballen sich bei ask 0,45–0,55 (WR 25%). **TP = +40%** (`updown_tuning.json`: `tp_frac 0.40` + `large_stake 100` → die fixe +$1,50-Übersteuerung via `tp_large_dollar` deaktiviert; Live `tp_pct 40`): höher ist schlechter — von 525 TP-Trades wären gehalten 61,5% Gewinner / 38,5% kippen → bis-Ende +1170$ < TP+40% +1291$. **Halte-bis-Ende-Schwanz (`resolved`+`hand`, −644$) = struktureller Münzwurf-Preis, NICHT sauber fixbar:** Spike-Filter Niete (schneidet Gewinner gleich mit), „vorher am Markt schließen" wirft den fetten `res_win`-Schwanz (+115% vs TP +40%) weg, Richtungsfilter unmöglich; `hand`-Exit = manuelle Dashboard-Closes (konfundiert, kein systematischer Exit). **ML-Richtung RE-bestätigt TOT (10.948 Binance-gelabelte Fenster):** AUC ~0,50 quer durch ALLE Features inkl. neuer btc_mom/cvd_accel/sym_mom (btc_cvd 0,513 best, LogReg OOS 0,498) → Klassifikator verworfen, Edge bleibt rein mechanisch. **FOK ist gelöst** (jüngste Live: 0 Fails, ~100% Fill via `SLIPPAGE 1.20`+Retry); das jüngste „FOK-Gefühl" war leeres Wallet (75× „Insufficient collateral"). **Redeem-Bug gefixt:** bullpen fragt seit Update interaktiv „Confirm redeem? [y/N]" → Cron-`real_stop_watch.py` hing seit 11.06. (löste keine gewonnenen Positionen ein) → `input="y\n"` ergänzt; das war der „abgelaufene lösen nicht auf"-Bug. Dashboard-Standalone-Keepalive `run_dash_only.sh` (wenn Master-Supervisor weg, Bot-Kinder halten aber den `run_dashboard.lock`).

**★ Richtungs-Vorhersage erschöpfend TOT (2026-06-13, mehrfach getestet — nicht neu aufrollen):** MACD+RSI auf die *zukünftige* Bewegung (t→Ende) = AUC ~0,50; die hohen „past"-AUCs (0,62–0,82) waren **Lookahead-Artefakt** (Indikator bei 60% Fenster spiegelt nur den schon gelaufenen Move). Cheap-Reversal 0,15–0,25 bounct **unter** Break-even (~11–19% Treffer bei ~20% nötig). **Up-only-billig** robust −EV (WR 0,17<0,20, beide Hälften rot); **Down-only-billig** nur ~Break-even & NICHT robust (Plus kam nur aus 1. Hälfte, 2. negativ; Vorsprung innerhalb Rauschen). Alle Chart-Indikatoren (MA/MACD/RSI/EMA/Bollinger/ADX) = dieselben **2 Wetten** (Momentum / Mean-Reversion), beide vom Markt fair bepreist; Fear&Greed aktualisiert täglich → intraday nutzlos; Whale-Tracking = einzige orthogonale Quelle, aber verrauscht/verzögert (CVD proxyt es schon, war 0,50). **Selbstlernen auf 0,50-Basis = Overfitting auf Rauschen → falsches Vertrauen (gefährlich), kein Feature.** Schwache Einzeltage (z.B. 13.06.: 5m +2,7 vs sonst +65/+80) = **Regime/Varianz** (15m+1h auch rot, obwohl ungeändert) → NICHT die Tuning-Änderungen, nicht an Einzeltagen reagieren. Edge bleibt rein **mechanisch**.

**★ Update 18.–19.06.2026 (diese Session):**
- **Up/Down-Paper crashte** $100 → Peak $362 (12.06.) → **$26**: in der Regime-Phase 15.–17.06. explodierte der `sl_deep`-Schwanz (183→471 Trades, −758→−1829$). **Markt/Regime, KEIN Bug** (Code seit 13.06. unverändert, Deep-SL −80% arbeitet wie geplant). Bestätigt die Fragilität: dünnes Netto zwischen TP-Gewinnen (+2851) und Schwanz-Verlusten (−2642).
- **★ MIN_ASK 0.40 eingebaut** (`updown_paper.py`, nach dem 5m-Filter; gilt alle TFs + Paper+Live via `_decide`): keine Einstiege < 0.40. Grund: billige <0.40-Longshots laufen überdurchschnittlich in den Deep-SL — 15m-Tiefenscan: `<0.40 = −103$` (der GANZE 15m-Verlust!), 15m bei ≥0.40 = +20$; global 0.30-0.40 = −55$. **Deployed + COMPILE_OK**, Backup `updown_paper.py.bak-0618`.
- **Up/Down-Paper RESET auf frische $100** (18.06.): alter State gesichert `updown_paper_state.json.pre-reset-0618`; Loop neu gestartet (Supervisor `run_updown.sh` = `while true` relauncht bei Kill). Misst jetzt die neuen Regeln sauber.
- **★★ FV-ERKENNTNIS (KEY — der bewährte Edge):** Der **Fair-Value-Touch/Barrier-Account** (`paper_state.json`) ist die *echte* Kante: $150 → **$347 (+384, ROI +4.9%/Trade)**, konsistente Equity-Kurve, und **überlebte das 15.–17.06.-Regime, das den Up/Down-Coinflip crashte** — weil FV ein **bepreisbares** Event handelt (vol-basierte Touch-Wahrscheinlichkeit) statt Richtung=Münzwurf. **BITC = Star (+466, +19% ROI)**, ETHE +37, **WTR (Wetter) = −118-Klotz** (trotz „Wetter raus 11.06."-Notiz wird WTR live noch gehandelt → verifizieren + DROPPEN). → **Energie in FV, Up/Down runterfahren.** (FV-Prinzip lässt sich NICHT auf Up/Down „retten": Up/Down nutzt schon `p`+`edge`; das Event *Richtung* ist unvorhersagbar; FV-Edge ist vol-basiert = preist Touches, nicht Richtung.)
- **Kronos raus = richtig** (CPU): hätte nicht geholfen (Richtung=Münzwurf; Kronos-getriebene 4h/daily waren ohnehin Verlierer). **4h/daily längst raus** (`TFS=["5m","15m","1h"]`).
- **„Sport-Teil" = nur Capability** (Dashboard-Label), **kein State-File, keine Sport-Trades** → ausprobieren = From-scratch-Paper-Test (WM ok für Daten), Erwartung niedrig (scharfer Markt).
- **★ Infra-Gotcha:** Der **laufende Bot liegt im WSL-NATIV-Heim `/home/k9l0v3r/polymarket-bot`** (kronos-venv), NICHT in der Windows-Kopie `C:\Users\petra\polymarket-bot` (die ist eine eingefrorene Kopie vom 15.06.!). Live-Analysen IMMER aus `/home/k9l0v3r/...` (z.B. `wsl cp` → `/mnt/c/...`, via **PowerShell** — Git-Bash verhackt `/home/`-Pfade). PowerShell→wsl→bash-Quoting: Parens/Alternation in `echo`/`grep` vermeiden.
- **★ TP-Config-Drift (verifiziert 19.06.):** Live `updown_tuning.json` = **`tp_frac: 1`** (nicht die dokumentierten **0.40**; war 0.65 früher in der Session → seither geändert). Echte Abweichung vom „TP +40% = optimal"-Befund → **prüfen/bewusst entscheiden** (tp_frac-Semantik: Gewinnschwelle vs. Verkaufsanteil klären; Daniel hat's evtl. selbst geändert). `tp_large_dollar: 1.5` ist gesetzt, aber via `large_stake: 100` bei $5-Stakes **dormant** (= wie Memo, kein Drift).
- **Nächstes Bot-Projekt:** [[shadow-resume-design]] (Always-On Equity-Curve-Gate, Coaching/Ultra).
